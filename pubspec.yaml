name: blb_entreprise
description: Application destinée au service BLB Transfert.

# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  # Icons de chargements
  flutter_spinkit: ^5.1.0
  # Firebase
  firebase_core: ^1.10.0
  firebase_auth: ^3.2.0
  cloud_firestore: ^3.1.0
  # Vérificateur d'emails
  email_validator: ^2.0.1
  # Messages d'avertissements
  fluttertoast: ^8.0.8
  # Informations des appareils utilisées
  device_info: ^2.0.2
  # Redimenssionnement du textes automatiquement
  auto_size_text_pk: ^3.0.0
  # Scanner des codes QR
  qr_code_scanner: ^0.6.1
  # Requêtes de permissions
  permission_handler: ^8.3.0
  # Icons supplémentaires
  font_awesome_flutter: ^9.2.0
  # Générateur de codes QR
  qr_flutter: ^4.0.0
  # Captures de widgets
  screenshot: ^1.2.3
  # Sauvegarde dans la gallerie du téléphone
  image_gallery_saver: ^1.7.0
  # Sélecteur de nombres
  numberpicker: ^2.1.1
  # Générateur de fichiers PDF
  syncfusion_flutter_pdf: ^19.2.59-beta
  # Ouverture de fichiers externes
  open_file: ^3.2.1
  # Barre de navigations
  custom_navigation_bar: ^0.8.2
  # Cartes extensibles
  expansion_tile_card: ^2.0.0
  # Boîte de dialogues / alertes
  rflutter_alert: ^2.0.4
  # Obtentions des emplacements couramment utilisés sur les systèmes de fichiers
  path_provider: ^2.0.8
  # Lancement d'URL
  url_launcher: ^6.0.6
  url_launcher_platform_interface: ^2.0.3
  # Drawer
  animated_drawer: ^0.0.4

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^1.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    # Icon Sign In Method
    - assets/iconSignIn.png
    # Transfert Administratif Images
    - assets/servicesImages/administratif.png
    - assets/servicesImages/transfertAdministratif/administratif1.png
    - assets/servicesImages/transfertAdministratif/administratif2.png
    # Transfert Industriel Images
    - assets/servicesImages/industriel.jpg
    - assets/servicesImages/industriel2.jpeg
    - assets/servicesImages/transfertIndustriel/industriel.jpg
    # Transfert Recyclage Images
    - assets/servicesImages/recyclage.png
    - assets/servicesImages/recyclage/valdelia.png
    - assets/servicesImages/recyclage/mobieco.png
    # Transfert Stockage Images
    - assets/servicesImages/stockage.jpg
    # Sponsor Images
    - assets/sponsorImage/afd.png
    - assets/sponsorImage/aldebaran.png
    - assets/sponsorImage/atradius.png
    - assets/sponsorImage/banquepopulaire.png
    - assets/sponsorImage/bdo.png
    - assets/sponsorImage/beaumanoir.png
    - assets/sponsorImage/bel.png
    - assets/sponsorImage/capsule.png
    - assets/sponsorImage/cms.png
    - assets/sponsorImage/cocacola.png
    - assets/sponsorImage/ecoemballage.png
    - assets/sponsorImage/eliorgroup.png
    - assets/sponsorImage/étandex.png
    - assets/sponsorImage/euronex.png
    - assets/sponsorImage/fidal.png
    - assets/sponsorImage/fnac.png
    - assets/sponsorImage/gefco.png
    - assets/sponsorImage/lafrançaise.png
    - assets/sponsorImage/leparisien.png
    - assets/sponsorImage/lesechos.png
    - assets/sponsorImage/loreal.png
    - assets/sponsorImage/louboutin.png
    - assets/sponsorImage/macif.png
    - assets/sponsorImage/mazars.png
    - assets/sponsorImage/mcsgroupes.png
    - assets/sponsorImage/mondialassistance.png
    - assets/sponsorImage/ordrenationaldesmedecins.jpg
    - assets/sponsorImage/pierre&vacances.png
    - assets/sponsorImage/primagaz.png
    - assets/sponsorImage/pwc.png
    - assets/sponsorImage/ricoh.png
    - assets/sponsorImage/rothschild&co.png
    - assets/sponsorImage/safran.png
    - assets/sponsorImage/solocalgroup.png
    - assets/sponsorImage/valdelia.png
    - assets/sponsorImage/vancleef&arpels.png
    - assets/sponsorImage/ysl.png
    # Card
    - assets/card/backgroundcard.png
    - assets/card/logoCardBLB.png
    # PDF Logo
    - assets/pdf/pdflogo.png

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
